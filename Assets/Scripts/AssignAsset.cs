using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AssignAsset : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        setMaterial();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

      void setMaterial()
    {
      
    }
}
